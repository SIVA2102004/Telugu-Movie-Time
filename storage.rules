rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can upload blueprint images, anyone can read them
    match /blueprints/{filename} {
      allow read: if true;
      allow write: if true;
    }
  }
}
