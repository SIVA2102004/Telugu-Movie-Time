export function sanitizeInput(input) { if (typeof input !== "string") return input; return input.replace(/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, "").replace(/[<>]/g, "").replace(/javascript:/gi, "").replace(/on\w+=/gi, "").trim(); } export function sanitizePhone(phone) { if (!phone) return ""; const cleaned = String(phone).replace(/\D/g, ""); return cleaned.slice(-10); } export function sanitizeUrlParam(param) { if (typeof param !== "string") return ""; return param.replace(/[^a-zA-Z0-9_-]/g, ""); } export function sanitizeSeatId(seatId) { if (typeof seatId !== "string") return ""; return seatId.toUpperCase().replace(/[^A-Z0-9]/g, ""); }
